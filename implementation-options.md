# 実装方式 比較検討書

> 作成: 2026-06-09

---

## 前提: 現在のサイト構成

**trip-guide.net は WordPress ではない。**

| 項目 | 現状 |
|---|---|
| フレームワーク | **Next.js 16 (App Router)** |
| 言語 | TypeScript |
| スタイリング | Tailwind CSS v4 |
| データ | JSON ファイル（静的・968施設） |
| ホスティング | Vercel |
| DB | **なし** |
| 認証 | **なし** |
| WordPress | **使っていない** |

---

## 比較する3つの選択肢

| | Option A | Option B | **Option C（推奨）** |
|---|---|---|---|
| 方式 | WordPress に移行してプラグインで実装 | WordPress に移行してテーマ内で実装 | **現在の Next.js に Supabase を追加** |

---

## 詳細比較

### Option A: WordPress プラグインとして実装

**概要**: 現在の Next.js サイトを廃止し、WordPress に全移行した上でプラグインで実装。

| 比較軸 | 評価 | 説明 |
|---|---|---|
| 初期費用 | △ | Xserver + WP テーマ代。現在の Vercel 無料枠より高コスト |
| 実装難易度 | **高（困難）** | Next.js → WP への全面移行が必要。現在のコードはすべて書き直し |
| 保守性 | △ | PHP ベース。WP の大量アップデート対応が必要 |
| 拡張性 | △ | プラグイン依存になりやすく、競合・廃盤リスクあり |
| セキュリティ | △ | WordPress は攻撃対象になりやすく、定期的な対応が必要 |
| ログイン管理 | △ | WP ユーザー管理。OAuth（Google 等）の追加が複雑 |
| 既存 SEO への影響 | **悪化リスク大** | URL 設計・OGP・サイトマップを再実装。移行期間中に順位低下の可能性 |
| AI 機能との相性 | **低** | pgvector・Edge 関数などが使えない |
| ホテル・習い事への拡張 | △ | プラグインで対応するが柔軟性低 |
| 海外展開 | **困難** | WP のマルチサイト構成は複雑 |

**結論**: **推奨しない。** 既存の高品質な Next.js 資産を捨てることになり、SEO リスク・移行コストが大きすぎる。

---

### Option B: WordPress テーマ内機能として実装

**概要**: WordPress に全移行し、テーマ内で独自実装。プラグイン依存を減らす方針。

| 比較軸 | 評価 | 説明 |
|---|---|---|
| 初期費用 | △ | Option A と同等 |
| 実装難易度 | **高（困難）** | テーマ開発の知識が必要。Option A より更に工数が増える |
| 保守性 | △ | WP バージョンアップのたびにテーマ修正が必要 |
| 拡張性 | △ | 独自実装なので柔軟だが、DB 設計・認証・API をすべて自作 |
| セキュリティ | △ | Option A 同様 |
| ログイン管理 | △ | WP ユーザー管理。OAuth 追加は複雑 |
| 既存 SEO への影響 | **悪化リスク大** | Option A 同様 |
| AI 機能との相性 | **低** | Option A 同様 |
| ホテル・習い事への拡張 | △ | 独自実装なので一定の柔軟性あり |
| 海外展開 | **困難** | Option A 同様 |

**結論**: **推奨しない。** Option A より実装コストが高く、利点がない。

---

### Option C: 現在の Next.js に Supabase を追加（推奨）

**概要**: 既存の Next.js + Vercel 環境を維持したまま、Supabase（PostgreSQL + Auth + Storage）を追加する。

| 比較軸 | 評価 | 説明 |
|---|---|---|
| 初期費用 | **◎** | Supabase 無料枠（50MB DB・50k MAU）で MVP は無料で開発可能 |
| 実装難易度 | **◎** | Next.js との統合が公式にサポートされており、学習コストが低い |
| 保守性 | **◎** | TypeScript で型安全。Next.js の既存コードと自然に統合 |
| 拡張性 | **◎** | PostgreSQL（SQL）なので将来の複雑なクエリに対応可能 |
| セキュリティ | **◎** | Row Level Security (RLS) がビルトイン。OAuth 2.0 標準対応 |
| ログイン管理 | **◎** | Supabase Auth がメール・Google・Apple ログインを標準サポート |
| 既存 SEO への影響 | **◎（影響なし）** | 既存ページは変更なし。マイページは `/mypage/*` として追加 |
| AI 機能との相性 | **◎** | pgvector が使えるため、将来の「類似施設推薦」「家族プロファイル分析」が実装可能 |
| ホテル・習い事への拡張 | **◎** | テーブル追加で対応可能 |
| 海外展開・多言語対応 | **◎** | 同一コードベースで多言語対応可。`country_code` / `currency_code` / `name_ja` / `name_en` フィールドをカラム追加で実装。別 Supabase プロジェクトで国別分離も可能 |
| 写真ストレージ（プライバシー対応） | **◎** | Supabase Storage + RLS で公開範囲（family_only / no_face_ok / anonymous_ok）をDBレベルで制御可能 |

**結論: Option C を強く推奨。**

---

## Option C の技術構成（詳細）

```
┌─────────────────────────────────────────┐
│  Next.js 16 (App Router) on Vercel      │
│                                          │
│  [既存] 施設検索・一覧・詳細（静的）     │
│  [新規] /mypage/* （サーバーコンポーネント + API Routes）│
│  [新規] /auth/* （ログイン・登録画面）  │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│  Supabase                                │
│  ├── Auth（メール / Google ログイン）    │
│  ├── PostgreSQL（visits / children etc）│
│  └── Storage（将来: 写真保存）          │
└─────────────────────────────────────────┘
```

### 使用ライブラリ

```json
{
  "@supabase/supabase-js": "^2.x",      // Supabase クライアント
  "@supabase/ssr": "^0.x",              // Next.js SSR 対応
  "next-auth": "（不要 — Supabase Auth で代替）"
}
```

### 費用感

| ティア | 費用 | 上限 |
|---|---|---|
| Free | **¥0** | DB 500MB / 50k MAU / 1GB Storage |
| Pro | $25/月（約 3,800円） | 無制限 MAU・8GB DB |

MVP 段階では無料枠で十分。ユーザーが 1,000人を超えてきたら Pro への移行を検討。

---

## 決定: Option C（Supabase + Next.js）で進める

### 理由サマリー

1. **既存資産を無駄にしない**: Next.js の高品質なコード・SEO・Vercel デプロイをそのまま使える
2. **移行リスクがない**: 既存の施設検索機能には一切手を加えない
3. **無料で始められる**: Supabase 無料枠で MVP を検証できる
4. **将来に強い**: pgvector で AI 検索、Storage で写真保存、RLS でプライバシー保護がすべて対応可能
5. **型安全**: TypeScript + Supabase の型自動生成で安全に開発できる

---

*最終更新: 2026-06-09*
