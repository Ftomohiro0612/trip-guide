# マイページ MVP 機能要件書 v3

> 作成: 2026-06-09 | ステータス: 承認待ち

---

## 1. ユーザーストーリー

```
親として、
  行った遊び場を記録したい。
  なぜなら、子どもが何に反応したかを忘れたくないから。

親として、
  過去に行った場所も遡って記録したい。
  なぜなら、子どもが小さい頃の思い出も残したいし、
  登録直後から自分の家族の履歴を作りたいから。

親として、
  次に行く場所を選ぶとき、過去の記録を振り返りたい。
  なぜなら、子どもに合う施設を選びたいから。

親として、
  行きたい場所を目的ごとに分けてリストに追加したい。
  なぜなら、「今週末」「夏休み」「雨の日」など目的によって候補が違うから。

親として、
  子どもひとりひとりの反応をボタンで記録したい。
  なぜなら、兄弟で好みが違うことがあるから。

親として、
  おでかけの写真を記録に添付したい。
  なぜなら、思い出として残したいから。ただし公開範囲を自分でコントロールしたい。
```

---

## 2. 入力体験の最重要原則

**「スマホで30秒以内に必須項目を入力できる」を最優先に設計する。**

| 原則 | 実装方針 |
|---|---|
| 必須項目は6項目のみ | すべてボタン・チェックボックスで選択 |
| 任意項目は非表示 | 「詳しく記録する▼」ボタンの中に隠す |
| テキスト入力は任意のみ | 自由メモ・子どもの一言など |
| 家族構成を自動反映 | 登録した子どもをチェックボックスで表示 |
| 途中保存対応 | 必須だけ先に保存、後から詳細を追加できる |
| 過去記録に対応 | 日付不明・年月のみでも登録可能 |
| デフォルト非公開 | 写真は「家族だけ見る」がデフォルト |

---

## 3. レビュー設計の考え方（将来分析から逆算）

レビュー項目は「なんとなく欲しいもの」を並べるのではなく、**後でどう使うかから逆算して設計する**。

### 最低限の必須6項目でできる分析

| 必須項目 | 使える分析 |
|---|---|
| 施設・訪問日 | 季節別人気度・リピート率・訪問履歴 |
| 今回行った子ども + 年齢 | 年齢別満足度・子どもの成長記録 |
| 子どもごとの満足度 | 子どもの好み分析・施設の年齢適合度 |
| また行きたいか | リピート率・似た家族レコメンドの精度 |
| 親の疲労度 | 「子どもは満足・親は疲れた施設」の検出 |

### 任意項目で集めるデータの用途

| 任意項目 | 使える分析 |
|---|---|
| 費用 | 家族の年間おでかけ費用・費用帯別おすすめ |
| 混雑・施設環境 | 施設改善レポート（B2B）・失敗回避情報 |
| 子どもの詳しい反応 | 好みカテゴリ分析・ホテル・習い事提案 |
| 期待を上回ったか | 穴場スポット発見・コスパ評価 |
| 自由メモ | テキストマイニング（将来）・個人的な思い出 |

---

## 4. 必須機能（MVP スコープ）

### 4-1. 認証

| 機能 | 詳細 |
|---|---|
| メール＋パスワード登録 | 基本認証 |
| Google ログイン | ソーシャルログイン（UX 向上） |
| パスワードリセット | メール送信 |
| ログアウト | セッション終了 |

Apple ログインは将来対応（iOS アプリ化時）。

### 4-2. マイページ（ダッシュボード）

- 訪問記録の件数・最近の記録
- 行きたいリストの件数（カテゴリ別）
- 子どもプロフィールの概要
- ボトムナビゲーション（記録する / 行きたいリスト / 履歴 / マイページ）

### 4-3. 子どもプロフィール

| 項目 | 必須 | 説明 |
|---|---|---|
| ニックネーム | ○ | 表示用（本名不要） |
| 生年月（年月のみ） | ○ | 訪問時の年齢を自動計算するため |
| 性別 | △ | 任意 |

- 複数人登録可能（兄・弟・姉・妹）
- 訪問記録の「誰と行ったか」の選択肢になる
- 生年月 × 訪問日 = 「当時の年齢」を自動計算

### 4-4. 訪問記録フォーム（おでかけログ）

---

#### 【必須】最低限レビュー（30秒で完了）

以下の6項目のみ入力して保存できる。詳細は後から追加可能。

**① 施設**  
施設名検索 または 施設ページから自動セット（優先）。

**② 訪問日**  
カレンダーで選択。以下のクイック選択ボタンも用意:
- 今日 / 昨日 / 先週末 / 今月 / 先月 / 日付不明

日付不明・年月のみ・年のみでも登録可能（詳細は §5 参照）。

**③ 今回行った子ども**  
登録済みの子どもをチェックボックスで表示。  
チェックを外すだけで「今回は行かなかった子ども」を除外できる。

**④ 子どもごとの満足度**（必須・ボタン選択）

チェックした子ども1人ずつに表示:

```
[大満足] [楽しんだ] [普通] [合わなかった] [参加できなかった]
```

- 1つのみ選択
- 「参加できなかった」を選択するとその子の詳細入力はスキップ

**⑤ また行きたいか（家族全体の評価）**（必須・ボタン選択）

```
[また行きたい] [条件が合えば行きたい] [一度で十分] [もう行かない]
```

**⑥ 親の疲労度**（必須・ボタン選択）

```
[楽だった] [普通] [少し疲れた] [かなり疲れた]
```

---

#### 【任意】詳細レビュー（「詳しく記録する▼」で展開）

**子どもの詳しい反応**（子どもごとに入力可）

| 項目 | 入力タイプ |
|---|---|
| 一番楽しんだもの | 自由記述 |
| すぐ飽きたもの | 自由記述 |
| 怖がったもの | 自由記述 |
| 物足りなかったもの | 自由記述 |
| 年齢的に早かった / ちょうどよかった / 遅かった | 3択ボタン |
| 兄弟で差が出たポイント | 自由記述 |

**期待を上回ったか（家族全体）**（準必須・将来の精度向上に有用）

```
[期待以上だった] [期待どおりだった] [期待以下だった]
```

→ 穴場発見・コスパ評価・旅行者向け失敗回避情報に使う。  
入力負担が増える場合は任意（または準必須）扱いでもよい。

**施設評価**

| 項目 | 入力タイプ |
|---|---|
| 混雑度 | 3択（空いてた / 普通 / 混んでた） |
| 到着時間 | 時間帯選択（朝 / 昼前 / 昼後 / 夕方） |
| 駐車場待ち | あり / なし |
| 待ち時間（遊具等） | 3択（なし / 少し / かなり） |
| 滞在時間 | セレクト（30分〜終日） |
| トイレ | 3択 |
| 授乳室 | 3択 |
| ベビーカー対応 | 3択 |
| 食事 | 3択 |
| 雨の日の使いやすさ | 3択 |
| 暑さ・寒さへの強さ | 3択 |
| 休憩場所 | あり / なし |

**費用**（入力してくれると年間家計分析・費用帯別おすすめに使える）

| 項目 | 入力タイプ |
|---|---|
| 費用合計（合計だけでも可） | 数値（円） |
| 入場料 | 数値（円） |
| 食事代 | 数値（円） |
| 交通費 | 数値（円） |
| 駐車場代 | 数値（円） |
| その他 | 数値（円） |

**自由入力**

| 項目 | 入力タイプ |
|---|---|
| 親のメモ | 複数行テキスト |
| 子どもの一言（印象に残った言葉） | テキスト |
| 次回行くなら注意したいこと | テキスト |
| 他の家族に伝えたいこと | テキスト |

**写真アップロード**（§6 参照）

---

### 4-5. 行きたいリスト（目的別ウィッシュリスト）

#### カテゴリ（目的タグ）

| カテゴリ | 説明 |
|---|---|
| 今週末に行きたい | 近日中の候補 |
| 雨の日候補 | 屋内・雨天でも使える場所 |
| 夏休み候補 | 夏の計画用 |
| 誕生日候補 | 特別な日の候補 |
| 祖父母と行きたい | 祖父母同伴向け |
| 旅行のとき | 遠出・宿泊旅行時の候補 |
| 近場で軽く | 気軽に行ける候補 |
| 未分類 | カテゴリ未選択 |

- カテゴリは後から変更可
- 施設ページから追加時にカテゴリ選択シートを表示

#### 一覧表示

- デフォルト: カテゴリ別グループ表示
- 切り替え: 追加日順 / 地域別
- 「行った！記録する」ボタンで訪問記録フォームへ遷移
- 削除・カテゴリ変更

### 4-6. おでかけ履歴一覧

- 訪問記録の一覧（過去記録も含む）
- 表示: 施設名・訪問日（または時期）・子どもの満足度アイコン・費用合計
- 並び替え: 新しい順 / 満足度順 / 費用順
- 子どもの年齢軸での絞り込み（将来：Phase 3 の年表機能と連動）
- 記録の編集・削除

---

## 5. 過去のおでかけ記録への対応

### 過去入力の導線

マイページ TOP または訪問記録一覧ページに「過去のおでかけを追加」ボタンを設置。  
通常の「今日のおでかけを記録」と並べて表示する。

### 日付入力 UI

```
[今日] [昨日] [先週末] [今月] [先月] [カレンダーで選ぶ] [日付不明]
```

過去の記録では正確な日付を覚えていないことがある。以下の精度を許容:

| 精度 | 例 | データ |
|---|---|---|
| 正確な日付 | 2024-08-15 | `visited_on = '2024-08-15'` |
| 年月のみ | 2024年8月ごろ | `visited_year=2024, visited_month=8` |
| 年のみ | 2024年ごろ | `visited_year=2024` |
| 不明 | 覚えていない | `date_precision='unknown'` |

カレンダーで選択した場合は「正確な日付」として扱う。  
「日付不明」でも施設・子ども・満足度が入っていれば記録として有効。

### 訪問時の子どもの年齢を自動表示

生年月が登録されていれば、訪問日を選択すると「当時の年齢」を自動計算して表示:

```
太郎（当時4歳）✓
花子（当時1歳）✓
```

日付不明の場合は年齢表示をスキップ（または手動入力を許可）。

### 過去入力モードの最低項目

過去記録では詳細を覚えていない可能性が高いため、さらに入力を絞る:

| 項目 | 過去入力モード |
|---|---|
| 施設 | 必須 |
| 訪問日または時期 | 必須（不明でも可） |
| 今回行った子ども | 必須 |
| 子どもごとの満足度 | 必須 |
| また行きたいか | 必須 |
| 親の疲労度 | 任意（覚えていない場合はスキップ可） |

「覚えていない」「入力しない」「後で入力する」の選択肢を随所に用意し、途中離脱を防ぐ。

---

## 6. 写真アップロード設計

### 公開範囲（必須選択・デフォルトは `family_only`）

| 設定値 | 意味 |
|---|---|
| 家族だけ見る（デフォルト） | 自分のアカウントのみ閲覧 |
| 顔なしなら公開利用可 | 顔が映っていない写真として施設紹介等に使用可（明示同意） |
| 匿名でOK | 子どもの情報を一切出さず匿名でサービスPR利用可（明示同意） |
| 非公開（下書き） | 一覧にも出さない |

### 顔写真の特別扱い

- アップロード時に「顔が写っていますか？」確認
- 顔あり写真を「公開利用可」設定にしようとした場合: 警告表示
- 施設向けに写真を使う場合は管理者承認制 or 個別同意制

---

## 7. 施設ページからの導線

### ログイン済みユーザー

施設詳細ページ（`/facilities/[slug]`）に追加:

```
[★ 行きたい！]  [✓ 行ったよ！記録する]
```

- 「行きたい！」→ カテゴリ選択シート → ウィッシュリストに追加
- 「行ったよ！記録する」→ `/mypage/visits/new?facility=<slug>` へ遷移（施設情報を自動セット）

### 未ログインユーザー

```
[★ 行きたい！] → ログインページへ（ログイン後に施設ページへ戻る）
```

---

## 8. ユーザーフロー

```
【新規登録後のフロー】
登録 → 子どもプロフィール登録 → マイページ TOP
  → 「過去のおでかけを追加」ボタンで複数件をまとめて登録
  → 最初から家族の履歴が完成した状態になる
  → 以降: 行くたびに30秒で記録

【既存サイト訪問者のフロー】
施設詳細を見る → 「行ったよ！記録する」をタップ → ログイン促進
  → 登録後に施設ページへ戻る → 記録フォームへ（施設名自動セット）
  → 必須6項目だけ入力（30秒）→ 保存 → マイページへ
```

---

## 9. 画面一覧

### ログイン前

| 画面 | URL |
|---|---|
| 施設検索（既存） | `/` |
| 施設詳細（既存 + ボタン追加） | `/facilities/[slug]` |
| ログイン | `/auth/login` |
| 新規登録 | `/auth/register` |
| パスワードリセット | `/auth/reset` |

### ログイン後

| 画面 | URL |
|---|---|
| マイページ TOP | `/mypage` |
| 子どもプロフィール | `/mypage/children` |
| 訪問記録一覧 | `/mypage/visits` |
| 訪問記録詳細 | `/mypage/visits/[id]` |
| 訪問記録追加 | `/mypage/visits/new` |
| 過去記録追加 | `/mypage/visits/new?mode=past` |
| 訪問記録編集 | `/mypage/visits/[id]/edit` |
| 行きたいリスト | `/mypage/wishlist` |
| アカウント設定 | `/mypage/settings` |

---

## 10. 後回し機能（MVP 後）

| 機能 | フェーズ |
|---|---|
| おでかけ履歴マップ | Phase 3 |
| 年間おでかけ費用集計 | Phase 3 |
| 子どもの好み分析・グラフ | Phase 3 |
| 思い出年表（訪問日軸の可視化） | Phase 3 |
| 期待超過スコアの施設ページ表示 | Phase 4 |
| 似た家族レコメンド | Phase 4 |
| 統計・失敗回避情報の施設ページ表示 | Phase 4 |
| AI おすすめ | Phase 7 |
| PDF アルバム出力 | Phase 6 |
| 習い事提案 | 将来 |
| ホテルログ詳細 | Phase 5 |
| SNS シェア | 検討 |
| 公開プロフィール | 検討 |

---

## 11. プライバシー・セキュリティ方針

- 子どものニックネームは外部公開しない（本名不要・ユーザー自身のみ閲覧）
- 生年月は年月のみ（日付まで不要）
- 居住地は都道府県または市区町村レベルまで（正確な住所は持たない）
- 学校名・園名は収集しない
- 写真のデフォルトは「家族だけ見る」
- 顔写真の公開利用は管理者承認制 or 個別同意制
- 写真の EXIF 情報（GPS・撮影日時）はアップロード時に除去
- 自由記述欄に個人情報が含まれる可能性への注意（利用規約で案内）
- 施設向け集計データは匿名化必須・利用規約で明示
- 個別家族の行動履歴を外部に見せない

---

*最終更新: 2026-06-09 v3*
